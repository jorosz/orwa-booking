# Másold `.env`-be és töltsd ki. A `.env` gitignore-olt; ez (.env.example) verziózott.
# A backuphoz kell (lásd BACKUP.md): a technikai IAM-user (csak s3:PutObject) kulcsa.
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=eu-central-1
BACKUP_BUCKET=orwa-booking-backup
